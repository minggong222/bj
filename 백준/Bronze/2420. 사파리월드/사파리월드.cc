#include <iostream>
#define ll long long
using namespace std;
ll n,m;
int main()
{
    cin.tie(NULL);
    cout.tie(NULL);
    ios::sync_with_stdio(false);
    cin >>n>>m;
    cout << abs(n-m);
    return 0;
}