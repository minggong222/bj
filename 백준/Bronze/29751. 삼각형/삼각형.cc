#include<iostream>
#include<queue>
#include<vector>
#include<stack>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    cout << fixed;
    cout.precision(1);
    cout << (float)(n * m) / 2;
    return 0;
}